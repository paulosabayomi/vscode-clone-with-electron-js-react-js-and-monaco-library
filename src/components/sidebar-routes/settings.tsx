import React from "react";

const SettingsComponent = React.memo((props: any) => {
    return (
        <div className="route">
            Settings route
        </div>
    )
})

export default SettingsComponent