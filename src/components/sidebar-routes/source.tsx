import React from "react";

const SourceComponent = React.memo((props: any) => {
    return (
        <div className="route">
            Source Control route
        </div>
    )
})

export default SourceComponent