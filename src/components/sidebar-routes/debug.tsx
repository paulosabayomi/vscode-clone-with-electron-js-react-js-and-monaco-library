import React from "react";

const DebugComponent = React.memo((props: any) => {
    return (
        <div className="route">
            Run and Debug route
        </div>
    )
})

export default DebugComponent