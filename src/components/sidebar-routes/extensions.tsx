import React from "react";

const ExtensionsComponent = React.memo((props: any) => {
    return (
        <div className="route">
            Extensions route
        </div>
    )
})

export default ExtensionsComponent