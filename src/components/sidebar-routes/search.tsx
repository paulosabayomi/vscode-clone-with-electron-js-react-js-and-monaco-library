import React from "react";

const SearchComponent = React.memo((props: any) => {
    return (
        <div className="route">
            Search route
        </div>
    )
})

export default SearchComponent