import React from "react";

const AccountComponent = React.memo((props: any) => {
    return (
        <div className="route">
            User account route
        </div>
    )
})

export default AccountComponent